?logging = true

model = Test_schemes
show (a, b)
a = 2
show (b)

model = Test_schemes { $model_scheme = "alt" }
show (a, b)
#b = 12
show (a)

model = Test_schemes { $model_scheme = "std" }
show (a, b)

